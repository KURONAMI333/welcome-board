# Gallery captions (Modrinth / CurseForge)

Uploaded per STORE_DESCRIPTION_GUIDE §7. All shots are the real in-game screen from
the sample content file, not mockups.

| File | Caption |
|---|---|
| `01_welcome_screen.png` | The screen a player sees on first join. Title, body, link buttons, and a close button — all from one JSON file. |
| `02_welcome_screen_26x.png` | The same content file on Minecraft 26.2. One file works across every supported version. |

## Not yet shot (add on a later release)

- The `/welcomeboard preview` command with a deliberate JSON typo, showing the warning in chat
- A content file with an image
- Two content files coexisting (`welcome.json` + `patch_notes.json`)
