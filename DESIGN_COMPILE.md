# Welcome Board — DESIGN_COMPILE（v0.1）

作成 2026-08-04。IDEA_TO_MOD STEP4 の必須8フィールド。
上流の選定根拠: `_research/sne-vs-tconstruct-2026-08/sne-vs-tconstruct.md`（オリジナル土俵の着工条件）。

---

## 1. コア体験1行

**modpack 作者が JSON を1つ置くだけで、プレイヤーの初参加時と pack 更新時に、ウェルカム画面が確実に出る。** 外部 mod は要らない。

## 2. スライス境界

### 空白の実証（着工根拠）

| 先行 | DL | 最終更新 | ローダー | 最大対応 |
|---|---:|---|---|---|
| Welcome Screen（nvb-uy） | MR 1,106,016（CF 570万+） | 2025-02-04 | fabric/forge/neoforge | 1.21.1 |
| WelcomeScreen（Globox1997） | MR 1,088,477 | 2024-09-10 | fabric | 1.21.1 |
| Modpack Core Essentials（おまけ機能） | 2,858 | 2026-07-27 | fabric/neoforge | 26.2 |

チャット1行の需要は Serilum が押さえている（Welcome Message 506,766 / First Join Message 399,483、いずれも 26.2 現役・4ローダー）。**空いているのはフルスクリーン GUI の側**で、合計220万 DL 分が 1.21.1 で止まっている。26.x で本格派なのは FancyDialogs / AcceptTheRules だが Paper/Bukkit プラグインで客層が別。

### 勝ち筋は機能ではなく「確実に出ること」

先行2系統の実ソースを読んだ結果、両者の GitHub Issue の筆頭が揃って「何も起きない」。

- **nvb-uy 版**: 自前の描画を持たず FancyMenu を呼ぶだけの薄いトリガー層。更新画面は BCC（Better Compatibility Checker）が無いと `getModpackVersion()` が空を返し続けて**永久に発火しない**
- **Globox 版**: `clearData()` を forEach の内側で呼んでおり複数ファイルが共存できない。パケット構築が `for (i < BUTTON_LIST.size())` で `IMAGE_LIST.get(i)` を読むため数が食い違うと `IndexOutOfBoundsException` で画面が出ない。`title`/`close` だけ存在チェック無しで、欠けた JSON は例外 → catch → ログのみで**完全に沈黙**
- **両者共通**: Origins の初回選択画面に上書きされる Issue が独立に2件、未解決のまま

### v0.1 に入る

| # | 項目 | 何の裏返しか |
|---|---|---|
| 1 | 外部 mod ゼロで動く（FancyMenu も BCC も不要） | nvb-uy の構造的失敗 |
| 2 | 内容ファイルが壊れていても画面は出す（欠損キーは既定値、壊れた箇所だけログ） | Globox の沈黙死 |
| 3 | 複数コンテンツファイルが id 指定で共存 | `clearData()` バグ / Issue #1 の複数ページ要望の前提 |
| 4 | 他の初回参加画面と喧嘩しない表示キュー | 2系統で独立に Issue 化された唯一の未解決点＝差別化 |
| 5 | 既読状態はサーバー／ワールド単位 | nvb-uy はインスタンス全体で1つ＝同じ pack で2つ目のサーバーに入ると永久に出ない |
| 6 | `revision` 整数1つで再表示（外部依存なし） | BCC 依存の除去 |
| 7 | アンカー配置（生ピクセル座標を書かせない） | 両者の `pos: [0, -40]` 方式 |
| 8 | テキスト / URL ボタン / 画像1枚 | — |

### v0.1 に入らない（スコープ外・明記）

- サーバー権威のコンテンツ配信（管理者がサーバー側で差し替えて全員に届く）→ v0.2
- 多言語ファイル分け（`<id>.<lang>.json`）→ v0.2
- ページ遷移・複数ページ → v0.2
- ボタンからのコマンド実行 → v0.2
- 画像複数枚 → v0.2
- 強制承諾ゲート（同意しないと kick）→ **作らない**。客層が別（Paper プラグイン側で AcceptTheRules 等が埋めている）で、「入れただけで効く」規律とも相性が悪い
- 汎用メニューエディタ → **作らない**。FancyMenu（208.6M DL）の領域

### 客の定義

**v0.1 の客は modpack 作者1本**。サーバー管理者向け（サーバー権威配信）は v0.2。
確認済み要因の2位が modpack 採用（CF dependents）で、pack 作者が自分の pack に入れる MOD は dependents が構造的に増える。この案を選んだ主因。

## 3. ユニット表

item/block/BE の登録を**一切しない**ので、NEW_MOD_GUIDE が「初の実 multiloader mod で確定する＝未検証」と書いている cross-loader 登録パターンの未踏を回避できる（§7 参照）。

| # | ユニット | 層 | 参照 | 予算 |
|---|---|---|---|---|
| U1 | コンテンツ JSON パーサ（耐障害・既定値補完） | **計算層**（MC 型ゼロ） | — | 120〜180 行 |
| U2 | レイアウト解決（アンカー → 実座標） | **計算層**（MC 型ゼロ） | — | 80〜120 行 |
| U3 | 既読状態ストア＋ revision 比較 | **計算層**（I/O だけ薄く分離） | — | 80〜120 行 |
| U4 | 画面描画（Screen 実装） | **描画層** | PLAYBOOK_CLIENT_RENDER | 150〜250 行 |
| U5 | 表示トリガー＋キュー（他画面との競合回避） | loader 固有 | Services / IPlatformHelper | 60〜100 行 |
| U6 | プレイヤー向け config（最小） | loader 固有 | NEW_MOD_GUIDE 実装慣例 | 20〜40 行 |
| U7 | lang 9言語（`de/en/es/fr/ja/ko/pt_br/ru/zh_cn`） | assets | NEW_MOD_GUIDE | — |

合計 510〜810 行。**PLAYBOOK_CLIENT_RENDER の絶対原則1に従い、U1〜U3 は Minecraft / NeoForge 型をゼロにする**（test source set にそれらはクラスパスに乗らないため、これはハード制約）。

### 設定の二層

- **プレイヤー向け config**: ほぼゼロ。画面を出すかどうかすら pack 側が決める。生の小数を並べない
- **pack 作者向けコンテンツファイル**: ここは製品面そのものなので表現力を持たせる。「設定は削れるだけ削る」（親 CLAUDE.md）は**プレイヤー向け config の規律**であって、こちらには適用しない

## 4. 進行グラフ

**該当なし。** クラフト・進行・レシピを持たない MOD。DESIGN_PROGRESSION の深さ相場は適用外。

## 5. 数値表

HP・攻撃力・処理時間・FE コストは該当なし。固定すべき数値はこれ。

| 項目 | 出発値 | 根拠 |
|---|---|---|
| 表示キューの待機上限 | 60 tick（3秒） | 他画面が閉じるのを待つ上限。超えたら諦めて次回に回す（沈黙死させない） |
| 表示トリガーの遅延 | 20 tick（1秒） | ワールド入場直後の他 mod の画面（Origins 等）が出揃うのを待つ |
| コンテンツファイルの最大サイズ | 64 KB | 壊れた／巨大なファイルでハングさせない上限 |
| ボタン数の上限 | 8 | 画面に収まる実用上限。超過分は無視してログ |
| テキスト行数の上限 | 32 | 同上 |

**レイアウト数値は確定させない。** アンカー方式の見え方は runClient でしか判定できない（§6）。プリセット3案を作って kura に1回で見比べさせる。

## 6. 検証手段 per ユニット

**前提**: PLAYBOOK_CLIENT_RENDER §4 —「JUnit はこの領域の欠陥を原理的に検出できない」。mod-061 では kura の却下が5回あり、そのすべてでテストは緑だった。計算層を固めることと、見え方を保証することは別。

| ユニット | 手段 | 肯定 / 否定の対 |
|---|---|---|
| U1 パーサ | **JUnit** | 肯定: 完全な JSON が全フィールド読める / 否定: `title` 欠損・型不一致・`buttons` が配列でない・64KB 超・不正 UTF-8 のいずれでも**例外を投げず、既定値で画面が出る形が返る** |
| U2 レイアウト | **JUnit** | 肯定: 各アンカーが期待座標に解決される（画面サイズ 320×240 / 854×480 / 1920×1080 の3解像度） / 否定: 要素が画面外に出ない（clamp が効く） |
| U3 既読・revision | **JUnit** | 肯定: 初回は表示・2回目は非表示・revision 増で再表示 / 否定: **サーバー A で既読にしてもサーバー B では表示される**（nvb-uy のバグの回帰テスト） |
| U4 描画 | **runClient 目視のみ**（機械判定できないと明記） | 候補シート行き |
| U5 キュー | **runClient**（Origins 等を入れた環境で実測）＋ 状態遷移だけ JUnit | 肯定: 他画面が開いていたら待つ / 否定: 60 tick 超で諦めて沈黙しない（ログを出す） |
| U6 config | runClient | — |

**U4 と U5 の実機部分は GameTest では取れない**（クライアント描画・他 mod との相互作用）。ここは runClient を必須ゲートにする。

## 7. 未踏フラグ

**あり（限定的）。** この repo で初の実 multiloader mod になる可能性がある。NEW_MOD_GUIDE §Flow M-2 が「登録（item/block/BE 等）パターンは cross-loader で未検証、初の実 multiloader mod で確定する」と明記している。

**ただし本 MOD は item/block/BE を一切登録しない**ので、その未踏部分を踏まない。踏むのは以下だけで、いずれもテンプレ同梱パターンで賄える範囲:

- クライアント画面の表示トリガー（NeoForge の client event ↔ Fabric の `ClientTickEvents`）→ `Services` + `IPlatformHelper` 越し
- config の登録（NeoForge `ModConfigSpec` ↔ Fabric 側の等価物）→ 同上
- リソース／ファイル読み込みパス（gamedir 相対）→ loader 非依存

登攀（別 HANDOFF 切り出し）は不要と判断する。**実装中にこの判断が誤りと分かったら GAP_LOG に書いて止める。**

## 8. kura ゲート計画

| ゲート | いつ | 何を見せるか |
|---|---|---|
| ① 解釈復唱 | **済**（2026-08-04、v0.1 スライスの提案として提示・GO 取得） | — |
| ② 候補シート（見た目） | U1〜U3 が JUnit 緑・U4 が runClient で1案出た時点 | **レイアウトのプリセット3案を1画面で並べた比較シート**。PLAYBOOK_CLIENT_RENDER §4 の「1案ずつ見せると往復が増える」に従い、必ず並置する |
| ③ ロゴ / アイコン | 公開前 | LOGO_PLAYBOOK（候補3+シート必須ゲート） |
| ④ 公開 | dev build 完成後 | RELEASE_PIPELINE Flow A |

**実機確認を頼む単位**: ②の候補シートの時に1回だけ。それ以外は runClient をこちら側で回して判定する（dev=prod 規律により実機テストはゲートにしない）。

---

## ターゲット

- **v0.1**: `multiloader-1.21.1`（NeoForge 1.21.1 + Fabric 1.21.1）— scaffold 済み・ビルド緑（2026-08-04）
- **v0.1 と同時に出す**: NeoForge 26.x（先行2本が 1.21.1 で止まっている＝**空白は 26.x 側にある**。1.21.1 だけ出しても nvb-uy 版と同じ土俵に並ぶだけで優位が立たない）
- **v0.2 以降**: Forge 1.20.1 + Fabric 1.20.1（`multiloader-1.20.1` の別プロジェクト）

## スコープ見積もり

510〜810 行 / 4〜6 作業日。当初「300〜500 行・2〜4日」と見積もったが、先行の実ソースを読んだ結果、表示キューと耐障害パーサを3ローダー × 2バージョンラインで通す分が乗る。
